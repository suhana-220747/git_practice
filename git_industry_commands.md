Command Name: git config --global user.name
Syntax: git config --global user.name "Your Name"
Purpose: Sets the global username for Git commits.
Example: git config --global user.name "Suhana"
Screenshot:
![git config email](group11.png)

Command Name: git config --global user.email
Syntax: git config --global user.email "your@email.com"
Purpose: Sets the global email for Git commits.
Example: git config --global user.email "suhana@email.com"
Screenshot:
![git config email](group11.png)

Command Name: git config --list
Syntax: git config --list
Purpose: Displays all Git configuration settings.
Example: git config --list
Screenshot:
![git config email](group11.png)

Command Name: git config --unset
Syntax: git config --unset user.name
Purpose: Removes a Git configuration setting.
Example: git config --unset user.name
Screenshot:
![git config unset](screenshots/git-config-unset.png)

Command Name: git init
Syntax: git init
Purpose: Initializes a new Git repository in the current directory.
Example: git init repo_1
Screenshot:
![git init](screenshots/git-init.png)

Command Name: git clone
Syntax: git clone <repository-url>
Purpose: Copies an existing repository from GitHub to the local system.
Example: git clone https://github.com/username/repo_1.git
Screenshot:
![git clone](group2.png)

Command Name: git clone --branch
Syntax: git clone --branch branch-name repository-url
Purpose: Clones a specific branch from a repository.
Example: git clone --branch dev https://github.com/username/repo_1.git
Screenshot:
![git clone branch](group2.png)

Command Name: git clone --depth
Syntax: git clone --depth 1 repository-url
Purpose: Performs a shallow clone with limited commit history.
Example: git clone --depth 1 https://github.com/username/repo_1.git
Screenshot:
![git clone depth](group2.png)Command Name: git init
Syntax: git init
Purpose: Initializes a new Git repository in the current directory.
Example: git init repo_1
Screenshot:
![git init](screenshots/git-init.png)

Command Name: git clone
Syntax: git clone <repository-url>
Purpose: Copies an existing repository from GitHub to the local system.
Example: git clone https://github.com/username/repo_1.git
Screenshot:
![git clone](group3.png)

Command Name: git clone --branch
Syntax: git clone --branch branch-name repository-url
Purpose: Clones a specific branch from a repository.
Example: git clone --branch dev https://github.com/username/repo_1.git
Screenshot:
![git clone branch](group3.png)

Command Name: git clone --depth
Syntax: git clone --depth 1 repository-url
Purpose: Performs a shallow clone with limited commit history.
Example: git clone --depth 1 https://github.com/username/repo_1.git
Screenshot:
![git clone depth](group3.png)

